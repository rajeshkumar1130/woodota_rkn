version: "3.9"
services:
  redis:
    image: redis:latest
    container_name: dota_redis
    hostname: dota_redis

  clarity:
    image: dota_parser:latest
    # image: arch1baald/clarity:${OS_PLATFORM:-linux}
    container_name: dota_parser
    ports:
      - "5600:5600"
      - "5005:5005"


  api:
    build: .
    container_name: dota_api
    env_file: .env
    environment:
      - REDIS_URL=redis://dota_redis
      - CLARITY_HOST=dota_parser
    ports:
      - "8000:8000"
      - "5678:5678"
    volumes:
      - ./src:/app/src
      - ./scripts:/app/scripts
      - ./replays:/app/replays
      - ./youtube:/app/youtube
    command: ["/bin/bash", "scripts/run_server.sh", "production", "INFO"]

  worker:
    build: .
    container_name: dota_worker
    env_file: .env
    environment:
      - REDIS_URL=redis://dota_redis
      - CLARITY_HOST=dota_parser
    volumes:
      - ./src:/app/src
      - ./scripts:/app/scripts
      - ./replays:/app/replays
      - ./youtube:/app/youtube
    command: ["/bin/bash", "scripts/run_workers.sh"]
